<%= form_with(model: piano_study) do |form| %>
  <% if piano_study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(piano_study.errors.count, "error") %> prohibited this piano_study from being saved:</h2>

      <ul>
        <% piano_study.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
